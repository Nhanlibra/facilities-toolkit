import React from 'react';

const LogoText = ({className}) => {
  return (
    <div className={className}>
      <h2 className="text-dark">FacilitiesPro</h2>
    </div>
  );
};

export default LogoText;
